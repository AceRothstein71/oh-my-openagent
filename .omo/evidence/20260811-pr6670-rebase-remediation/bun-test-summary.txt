(pass) auto-update checker catch fallbacks > returns null when local dev version read throws a non-Error [0.81ms]

packages/omo-opencode/src/hooks/auto-update-checker/hook/background-update-check.test.ts:
(pass) createBackgroundUpdateCheckRunner — OpenCode-managed sandbox (#4318) > #given import.meta.url resolves outside the flat cache workspace and config-dir #when auto-update fires with autoUpdate=true #then bun install is skipped and only the update-available toast is shown (not 'Updated!') [0.49ms]
(pass) createBackgroundUpdateCheckRunner — OpenCode-managed sandbox (#4318) > #given import.meta.url resolves inside the flat cache workspace #when auto-update fires with autoUpdate=true #then existing install flow runs unchanged [0.27ms]
(pass) createBackgroundUpdateCheckRunner — OpenCode-managed sandbox (#4318) > #given bun install throws a non-Error #when auto-update fires #then it falls back to update-available notification [0.20ms]

packages/omo-opencode/src/hooks/keyword-detector/ultrawork/ultrawork-source-routing.test.ts:
(pass) Ultrawork source routing > #given agent and model #then getUltraworkSource routes to the expected variant [0.20ms]

packages/omo-opencode/src/hooks/claude-code-hooks/handlers/session-event-handler-retry.test.ts:
(pass) createSessionEventHandler retry behavior > #given transient parent lookup failure #when the next idle succeeds #then stop hooks receive the later parent session id [0.73ms]
(pass) createSessionEventHandler retry behavior > #given parent lookup throws a non-Error value #when the next idle succeeds #then stop hooks receive the later parent session id [0.13ms]
(pass) createSessionEventHandler retry behavior > #given a parentless idle session #when stop hooks execute #then the context carries the session transcript path [0.12ms]

packages/omo-opencode/src/hooks/claude-code-hooks/handlers/session-event-handler.test.ts:
(pass) createSessionEventHandler > #given deleted session with retained caches #when session deleted arrives #then per-session resources are cleared [3.05ms]
(pass) createSessionEventHandler > #given active singleton state #when dispose runs #then all shared caches are cleared [0.46ms]
(pass) createSessionEventHandler > #given repeated idle events for one session #when stop hook preparation runs #then parent session lookup is reused [0.12ms]
(pass) createSessionEventHandler > #given deleted session #when it idles again #then parent session lookup is fetched again [0.11ms]

packages/omo-opencode/src/hooks/claude-code-hooks/handlers/tool-execute-before-handler.test.ts:
(pass) createToolExecuteBeforeHandler > #given Bash has an active tool cwd #when handler runs #then PreToolUse receives that cwd [1.45ms]
(pass) createToolExecuteBeforeHandler > #given Bash lacks an explicit cwd but session has a tracked worktree #when handler runs #then PreToolUse receives the tracked worktree [0.15ms]
(pass) createToolExecuteBeforeHandler > #given todowrite JSON parsing throws a non-Error value #when handler runs #then it reports the same parse error [0.15ms]
(pass) createToolExecuteBeforeHandler > #given denial toast rejects with a non-Error value #when hook denies #then the hook denial still wins [0.15ms]

packages/omo-opencode/src/hooks/claude-code-hooks/handlers/tool-execute-after-handler.test.ts:
(pass) createToolExecuteAfterHandler > #given diff-heavy metadata #when transcript entry is appended #then it keeps concise output with compact metadata [0.61ms]
(pass) createToolExecuteAfterHandler > #given multiline PostToolUse context on empty tool output #when output is appended #then it renders clean normalized sections [0.17ms]
(pass) createToolExecuteAfterHandler > #given warning toast rejects with a non-Error value #when PostToolUse blocks #then output still receives hook sections [0.12ms]

packages/shared-skills/skills/programming/scripts/typescript/check-no-excuse-rules.test.ts:
(pass) #given the no-excuse script is executed from an installed ~/.codex-style skill cache > #when the caller project provides typescript #then it resolves the caller's typescript and exits 0 [161.67ms]
(pass) #given the no-excuse script is executed from an installed ~/.codex-style skill cache > #when a checked file violates the rules #then violations are still reported with exit 1 [83.32ms]
(pass) #given the no-excuse script is executed from an installed ~/.codex-style skill cache > #when the caller project genuinely lacks typescript #then it fails with a clear error and exit 2 [20.88ms]

11 tests skipped:
(skip) bounded sg NDJSON runner > #given a hung child on Windows #when aborted #then child.kill terminates it without POSIX grace signaling
(skip) bounded sg NDJSON runner > #given a hung child on Windows #when the whole-call budget expires #then child.kill terminates it without POSIX grace signaling
(skip) omo setup sibling detection > #given all four harness stores > #when detected #then the exact report contains ids and types but no values
(skip) omo setup sibling detection > #given all four harness stores > #when detection runs twice #then every source hash and directory listing stays identical
(skip) omo setup sibling detection > #given each store is absent in turn > #when detected #then that harness reports installed no without crashing
(skip) omo setup sibling detection > #given an unknown sqlite auth schema > #when detected #then it reports detection-only and parses no credentials
(skip) omo setup sibling detection > #given node:sqlite is unavailable > #when detected #then sqlite stores degrade to file presence and remain successful
(skip) omo setup credential inheritance > #given pinned omp and gjc databases #when accepted #then allow-listed rows import and unknown schema is noticed
(skip) team-mode live tmux smoke > #given a real caller tmux session and two mock members #when createTeamLayout runs #then teammate panes appear in the caller window and cleanup leaves the session intact
(skip) reapLspDaemons live ownership > #given a live Windows named pipe legacy daemon #when reaping #then it defers without sending POSIX signals
(skip) team-mode live tmux smoke > #given a real caller tmux session and two mock members #when createTeamLayout runs #then teammate panes appear in the caller window and cleanup leaves the session intact

 14287 pass
 11 skip
 0 fail
 1 snapshots, 78890 expect() calls
Ran 14298 tests across 1855 files. [272.17s]
